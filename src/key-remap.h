//Remaps keys on calculator independent of PC
//For remapping keys on PC to calculator keys, see key-remap-pc.h

#ifndef __GUARD_PROG_TOOLS_KEY_REMAP
    #define __GUARD_PROG_TOOLS_KEY_REMAP

    //Remap keys on calculator keypad for keys that don't exist like : and !
    int forth_key_remap();
    int shell_key_remap();
#endif
