//Remaps keys on PC to calculator keys
//For remapping keys on calculator independent of PC, see key-remap.h

#ifndef __GUARD_PROG_TOOLS_KEY_REMAP_PC
    #define __GUARD_PROG_TOOLS_KEY_REMAP_PC

    //Remap keys on PC only for easier debugging. Not used on calculator.
    extern struct KeyRemap remapped_keys_all[];
    extern struct KeyRemap forth_keys[];
#endif
